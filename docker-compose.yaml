services:
  imap-notifier-server:
    restart: unless-stopped
    container_name: imap-notifier-server
    ports:
      - 7140:7140
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - config_volume:/app/config
    environment:
      - TZ=Europe/Berlin
volumes:
  config_volume:
